extends layout/main-layout.pug

block content
    header.nav.d-flex.justify-content-between.align-items-center.container-page 
      a.navbar-brand(href="/") 
        sup M  &#9829;
        sub Y
      ul.nav.justify-content-end
        li.nav-item
          li.active
            a(href="/") 
              i.bi.bi-house
        li.nav-item
          a.active(href="#") Undangan
        li.nav-item
          a.active(href="/married/reception") Resepsi
        li.nav-item
          a(href="#") Peta
        li.nav-item
          a(href="#") Buku Tamu
    
    main 
      form(action="/add-message", method="POST") 
        div 
          label(for="name") Nama
          input(type="text", id="name", name="name" placeholder="Masukan Nama")
        div 
          label(for="message") Pesan
          input(type="text", id="message", name="message", placeholder="Masukan Pesan")
        button(type="Submit") Submit Pesan


    main
      if messages.length > 0
        each message in messages
          p #{message.name}
          p #{message.date}
          p #{message.message}
      else 
        h1 Tidak Ada Pesan


    